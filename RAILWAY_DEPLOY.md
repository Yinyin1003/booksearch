# Railway 部署指南（最简单）

## 🚀 Railway 一键部署

Railway 是最简单的部署方式，无需配置，自动检测 Python 项目。

## 📋 完整步骤

### 步骤 1: 准备代码（如果还没推送到 GitHub）

```bash
cd /Users/zhouyinyin/Downloads/booksearch1

# 初始化 Git（如果还没有）
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub（如果还没有）
git remote add origin https://github.com/你的用户名/booksearch.git
git branch -M main
git push -u origin main
```

### 步骤 2: 部署到 Railway

1. **访问 Railway**
   - 打开 https://railway.app
   - 点击 "Start a New Project"

2. **登录**
   - 选择 "Deploy from GitHub repo"
   - 使用 GitHub 账号登录
   - 授权 Railway 访问你的 GitHub

3. **选择仓库**
   - 在仓库列表中找到 `booksearch`（或你的仓库名）
   - 点击仓库

4. **自动部署**
   - Railway 会自动检测到 Python 项目
   - 自动安装依赖（从 `requirements.txt`）
   - 自动启动应用（检测到 `app.py`）
   - 等待 2-3 分钟部署完成

5. **获取 URL**
   - 部署完成后，Railway 会显示一个公开 URL
   - 例如：`https://booksearch-production.up.railway.app`
   - 点击 URL 旁边的图标可以复制

### 步骤 3: 配置（可选）

Railway 通常不需要任何配置，但如果需要：

1. **查看设置**
   - 点击项目 → Settings

2. **环境变量（可选）**
   - 点击 Variables
   - 添加：`FLASK_DEBUG=False`（生产环境）

3. **域名（可选）**
   - 点击 Settings → Generate Domain
   - 或添加自定义域名

### 步骤 4: 上传书架图片

部署完成后，访问你的网站：

1. **通过 Web 界面上传**
   - 访问部署后的网站
   - 在管理界面使用图片上传功能上传 `bookshelf.jpg`

2. **或通过 Git 提交**
   ```bash
   git add bookshelf.jpg
   git commit -m "Add bookshelf image"
   git push
   ```
   Railway 会自动重新部署

## ✅ 完成！

现在你的网站已经上线了！

访问你的 Railway URL，例如：
```
https://booksearch-production.up.railway.app
```

## 🔧 如果遇到问题

### 问题 1: 部署失败

**查看日志**：
- Railway Dashboard → Deployments → 点击最新的部署
- 查看 Build Logs 和 Deploy Logs
- 寻找错误信息

**常见错误**：
- **ModuleNotFoundError**: 检查 `requirements.txt` 是否包含所有依赖
- **Port error**: Railway 会自动设置 PORT，不需要手动配置
- **Build timeout**: 免费计划有构建时间限制，可能需要升级

### 问题 2: 网站无法访问

**检查**：
1. 部署是否成功（查看 Deployments）
2. 服务是否运行（查看 Metrics）
3. 日志中是否有错误

### 问题 3: 功能不工作

**检查**：
1. 书架图片是否已上传
2. 浏览器 Console（F12）查看错误
3. 后端日志查看 API 错误

## 📊 Railway 免费额度

- ✅ **$5 免费额度/月**
- ✅ 足够运行小型应用
- ✅ 超出后按使用量付费

## 🎯 推荐配置

Railway 会自动检测配置，通常不需要手动设置：

- **Build Command**: 自动检测（`pip install -r requirements.txt`）
- **Start Command**: 自动检测（`python app.py` 或 `gunicorn app:app`）
- **Port**: 自动设置 `PORT` 环境变量

## 🔄 更新代码

以后更新代码时：

```bash
git add .
git commit -m "更新说明"
git push
```

Railway 会自动检测到更改并重新部署！

## 💡 提示

1. **监控使用量**
   - Railway Dashboard → Usage
   - 查看免费额度使用情况

2. **查看日志**
   - Railway Dashboard → Deployments → Logs
   - 实时查看应用日志

3. **重启服务**
   - 如果出现问题，可以手动重启
   - Railway Dashboard → 点击服务 → Restart

## 🎉 完成！

你的应用现在已经在 Railway 上运行了！

访问你的网站，测试功能：
- 管理界面：`https://your-app.railway.app/`
- 语音预览：`https://your-app.railway.app/preview`

---

**需要帮助？** 查看 Railway Dashboard 的日志或联系我！

